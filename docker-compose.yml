services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: advisorplatform
      POSTGRES_USER: advisorplatform
      POSTGRES_PASSWORD: advisorplatform
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
